# 04 — 当前状态与路线图

> 日常参考。线二刚开始，状态简洁。

## 总体进度

```
Phase 0  ⏳ 环境搭建   WSL conda 环境 + GitHub 仓库 + 目录结构
Phase 1  ⏳ 数据准备   从 H5 源生成独立 cache（RF + baseline B-mode + GT B-mode）
Phase 2  ⏳ 架构实现   双支路编码器 + 融合 + 解码器（Light3DUNet 变体）
Phase 3  ⏳ 训练基线   50 epoch 纯监督，验证双输入是否优于纯 RF
Phase 4  ⏳ 评估       全容积拼接 + NIfTI + 3D Slicer
```

## 当前阻塞项

| 阻塞项 | 状态 | 备注 |
|--------|------|------|
| WSL conda 环境 | 🔴 待建 | 需要 rf-cgan-clean 环境（Python 3.10 + PyTorch 2.11+cu128） |
| H5 源数据访问 | 🔴 待确认 | 从 Linux 服务器 scp/rsync 到 WSL，或 NFS/SMB 挂载 |
| 独立 cache 生成 | 🔴 待做 | 需要 baseline B-mode 全量转换（线一仅转了 label） |
| 双输入架构设计 | 🔴 待做 | 规格待 Claude 决策 |

## 数据从哪来

线二和线一同源——1500 个 H5 patches 在 Linux 服务器上：
`/media/liujia/rf_pipeline/64x32x32/02_h5_patches/`（567G）

线二不能直接读线一的 cache（工程隔离），需要从同一个 H5 源生成自己的 cache。
生成路径在 WSL 本地：`G:\rf_training_cache\dual_input_bmode\`

**数据获取方式**（待用户确认）：
- 方案 A：WSL 直接挂载 Linux 的 SMB/NFS 共享
- 方案 B：scp/rsync 将 H5 源拷贝到 WSL 本地
- 方案 C：在 Linux 上生成 cache 后通过网络拷贝到 WSL

## 下一步

1. 建 WSL conda 环境
2. 确认 H5 数据获取方案
3. 生成独立 RF cache + baseline B-mode cache + GT B-mode cache
4. Claude 出双输入架构设计规格
5. Codex 实现架构并训练
