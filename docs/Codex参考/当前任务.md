# 当前任务

> 给线二的 Codex。现在要做什么。最后更新: 2026-06-12。

## 🔴 Phase 1：环境搭建

**状态**：待执行。

1. 在 WSL 中创建 conda 环境（Python 3.10, PyTorch 2.11+cu128）
2. 确认 GPU 可访问（nvidia-smi）
3. 确认项目目录结构正确

## 🔴 Phase 2：数据准备

**状态**：待 Claude 指令 + Codex 执行。

1. 从 Linux 服务器获取 H5 源数据（scp/rsync 或 NFS 挂载）
2. 在本地生成独立 cache：RF input + baseline B-mode + label B-mode
3. baseline B-mode 全量 1500 转换（五步，REF=64407.58）
4. Proma 独立交叉验证 cache 质量

### 数据路径

| 数据 | 源（Linux） | 目标（WSL） |
|------|------------|-------------|
| H5 patches | `ssh liujia@liujia-Linux:/media/liujia/rf_pipeline/64x32x32/02_h5_patches/` | 本地 |
| RF cache | — | `G:\rf_training_cache\dual_input_bmode\` |
| B-mode GT | — | `G:\rf_training_cache\dual_input_bmode\` |
| B-mode baseline | — | `G:\rf_training_cache\dual_input_bmode\` |

### 服务器访问

```bash
ssh liujia@liujia-Linux   # 密码: Liujia
```

## 🔴 Phase 3：架构实现

**状态**：待 Claude 出架构规格。

将实现双支路编码器 + 融合 + 解码器。具体规格由 Claude 在 `workspace-files/.context/` 中提供。
