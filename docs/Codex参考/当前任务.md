# 当前任务

> 给线二的 Codex。现在要做什么。最后更新: 2026-06-13。

## 🟢 Phase 0：环境搭建（完成）

**状态**：已完成（2026-06-13）。

1. ✅ WSL conda 环境 **rf-bmode**（torch 2.5.1+cu121, RTX 4060）— 旧版误写 rf-cgan-clean，本 WSL 无该环境
2. ✅ GPU 可访问（nvidia-smi）
3. ✅ 项目目录结构正确

## 🟢 Phase 1：数据准备（完成）

**状态**：已完成（2026-06-13）。Proma 独立验证 PASS。

cache 路径：`G:\rf_training_cache\dual_input_bmode\`（WSL 内 `/mnt/g/rf_training_cache/dual_input_bmode/`）
train=1050 / val=225 / test=225，SHA256 全通过。

## 🟢 Phase 2：基础设施（reader 完成，探针待做）

### 2a. cache reader — ✅ 完成

`rf_cached_dataset.py`（仓库根）+ `scripts/smoke_test_cached_dataset.py`。
smoke test `ALL_SMOKE_PASS`，Claude 读码签收（memmap 逐样本、可 pickle、num_workers 安全）。

### 2b. I/O 吞吐探针 — ✅ 完成

`scripts/probe_dataloader_throughput.py`。实测 num_workers=4→~8.4min/epoch、=8→~5.3min/epoch（偏乐观，见路线图）。worker>0 不崩。

### 2c. 3×3 可视化探针 — 🟡 并入训练 notebook

探针图规范（在 `train.ipynb` 里实现，每 epoch 输出）：
- 默认 `y=16` 切片；train/val 各一张合并图
- 3 行：carotid / muscle / phantom；3 列：**baseline | pred | gt**（列顺序统一）
- 三类样本训练前固定，写入 `01_config/`
- Z 轴按物理 aspect ratio 显示（axial 0.0362mm / lateral 0.2mm）

## 🟢 Phase 3：架构实现（模型完成 ✅，训练待做）

**模型 + 验证 PASS**（2026-06-13）：`DualBranchBModeUNet`（7.34M 参数），Codex 实现 + Proma 独立 9 项验证交叉一致，显存 fwd+bwd 3.42G << 8G。详见 `项目笔记/05_实验记录.md` Phase 3。

**下一步：首个训练运行**（严格按 `原则与规范/04_实验规范.md`）：
- 运行目录 `experiments/dual_input_v1/<YYYY-MM-DD_short_name>/`，四阶段（01_config / 02_train / 03_validate / 04_proma_verify）
- **强制 `train.ipynb`（禁 .py 训练脚本）**，冻结 `config.yaml` + `run_manifest.json`（记 git commit）
- 每 epoch：探针图 3×3 + `stability.csv`（loss/SSIM3D/L1）+ 散斑 SNR 分类别
- Claude 正在出训练指令。
